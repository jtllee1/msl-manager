<div class="container new">
  <%= simple_form_for([@astromon, @third_trinket]) do |f| %>
    <div class="gem-cards">
      <% @third_trinkets.each do |trinket| %>
        <% if trinket.available %>
          <label>
            <%= check_box_tag "trinket", trinket.id, false, class: "invisible" %>
            <%= render "shared/astromon-trinkets-three", trinket: trinket %>
          </label>
        <% end %>
      <% end %>
    </div>
    <%= f.submit "Add Third Trinket", class: "button" %>
    <%= link_to "Back", astromons_path, class: "button" %>
  <% end %>
</div>
